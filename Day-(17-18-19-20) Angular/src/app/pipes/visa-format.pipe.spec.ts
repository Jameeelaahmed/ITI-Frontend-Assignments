import { VisaFormatPipe } from './visa-format.pipe';

describe('VisaFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VisaFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
